# This file ensures the data directory is created in git but keeps database files ignored
# The SQLite database (webui.sqlite) will be auto-created by the application
# Do not commit actual database files - they contain user data and secrets
